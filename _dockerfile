FROM node:lts-alpine
# FROM alpine:3.7



RUN apk update 
# RUN apk update && apk add --no-cache mysql-community-server
# RUN apk add --no-cache node.js
# RUN apk add --no-cache mysql-server

# COPY init.sql /docker-entrypoint-initdb.d/
# CMD mysql {mysql-client-options} < {sql.script}

COPY . /usr
WORKDIR /usr

# RUN ["--privileged" "--name my-app" "-p 8080:8080" "-p 3306:3306" "-it -d"]



# #"--default-authentication-plugin=caching_sha2_password"
# CMD ["--default-authentication-plugin=mysql_native_password"]
# ENV default-authentication-plugin=mysql_native_password
# ENV MYSQL_ROOT_PASSWORD=root-psw
# ENV MYSQL_USER=client MYSQL_PASSWORD=client-psw

# EXPOSE 3306
# EXPOSE 33060
