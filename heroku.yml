build:
  docker:
    web: ./dockerfile
    mysql_db: ./mysql-db/dockerfile
  config:
    MYSQL_ROOT_PASSWORD: root-psw
    MYSQL_USER: "client"
    MYSQL_PASSWORD: "client-psw"

# release:
#   image: mysql_db
#   command: some command that get executed before run

run:
  web: npm run build && npm start
  mysql_db: MYSQL_ROOT_PASSWORD=root-password
